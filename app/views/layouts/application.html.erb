<!DOCTYPE html>
<html>
<head>
  <title>Efgame</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

<body>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<%= yield %>

<div class="navbar navbar-inverse navbar-fixed-top ">
<div class="navbar navbar-inverse navbar-fixed-top colour">
  <div class="container ">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"></a>
    </div>
    <div class="navbar-collapse collapse alignment" >
      <ul class="nav navbar-nav colour">
        <li class="active"><a href="../../profiles/show">خانه</a></li>
        <li><a href="../../games">بازی ها</a></li>

      </ul>
    </div><!--/.nav-collapse -->

    <li class="name">
      <% if user_signed_in? %>
          <a href="../../users/edit"><%= current_user.name%></a>
      <% else %>
          <a href="../../users/sign_in">ورود</a>

    <% end %>
    </li>

    <% if user_signed_in? %>
        <div class ="khoruj">
          <%= link_to "خروج", destroy_user_session_path, :method => :delete %>
        </div>
    <% end %>
  </div>
</div>
</div>
</body>
</html>
